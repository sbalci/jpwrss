
# This file is automatically generated, you probably don't want to edit this

zlogregOptions <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "zlogregOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            calculate = "selectpower", ...) {

            super$initialize(
                package="jpwrss",
                name="zlogreg",
                requiresData=FALSE,
                ...)

            private$..calculate <- jmvcore::OptionList$new(
                "calculate",
                calculate,
                options=list(
                    "selectpower",
                    "selectsamplesize"),
                default="selectpower")

            self$.addOption(private$..calculate)
        }),
    active = list(
        calculate = function() private$..calculate$value),
    private = list(
        ..calculate = NA)
)

zlogregResults <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "zlogregResults",
    inherit = jmvcore::Group,
    active = list(
        text2 = function() private$.items[["text2"]],
        plot = function() private$.items[["plot"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Logistic Regression Single Coefficient (Large Sample Approx Wald’s z Test)",
                refs=list(
                    "pwrss"))
            self$add(jmvcore::Preformatted$new(
                options=options,
                name="text2",
                title="One Proportion z test"))
            self$add(jmvcore::Image$new(
                options=options,
                name="plot",
                title="A proportion against a Constant (z Test)",
                renderFun=".plot"))}))

zlogregBase <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "zlogregBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = "jpwrss",
                name = "zlogreg",
                version = c(1,0,0),
                options = options,
                results = zlogregResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE)
        }))

#' Logistic Regression Single Coefficient (Large Sample Approx Wald’s z Test)
#'
#' 
#' @param calculate .
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$text2} \tab \tab \tab \tab \tab a preformatted \cr
#'   \code{results$plot} \tab \tab \tab \tab \tab an image \cr
#' }
#'
#' @export
zlogreg <- function(
    calculate = "selectpower") {

    if ( ! requireNamespace("jmvcore", quietly=TRUE))
        stop("zlogreg requires jmvcore to be installed (restart may be required)")


    options <- zlogregOptions$new(
        calculate = calculate)

    analysis <- zlogregClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}

